{
	"package": {
		"link": "https://api.github.com/repos/tailscale/tailscale/releases/latest",
		"search": "grep 'tag_name' | awk '{print substr($2, 2, length($2)-3) }'",
		"message": "Tailscale",
		"currentversion": "NONE"
  }
}
